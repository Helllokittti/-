print ("Введите координаты точки: ")
x = float(input("Координата x: "))
y = float(input("Координата y: "))
while 0 > y or y > 1 :
        y -= 2 * abs(y) // y
while abs(x) > 1 :
        x -= 2 * abs(x) // x
print("x=",x,'\n',"y=",y)
if abs(x) <= 1 and 0 <= y <= 1 - abs(x) :
        print("Точка в области")
else:
    print ("Точка не в области")
